# API contracts

Stack-agnostic API definitions shared by all frontend and backend implementations.

Planned contents:
- `openapi.yaml` — REST endpoints (`/api/Account`, `/api/Lobbies`)
- `signalr.md` — real-time hub events and commands (`/PredatorWebsocket`)

Current reference implementation: `frontends/angular` + `backends/dotnet-webapi`.

# Docker

Docker Compose profiles for running different frontend/backend stack combinations.

Planned profiles:
- `angular-dotnet` — Angular + ASP.NET Core (default)
- `react-dotnet` — React + ASP.NET Core
- `react-node` — React + Node.js

Coming in a later phase.
